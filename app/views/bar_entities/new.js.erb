$('#new_link').fadeOut().prependTo(".bar-list").after('<%= j render("profile_form") %>');

$("#source")
    .appendTo("#destination");

$(document).foundation();

$('form').each(function() { $(this).validate(); });